[tox]
envlist = py310
skipsdist = True 

[testenv]
deps = -r{toxinidir}/snitch/requirements.txt
commands =
        coverage run --source . --omit .tox/* -m unittest discover -p "*" ./tests
        coverage report